# Screenshots

Place screenshot images here. The main README references:

- `command.png` — Command page showing natural language input and results
- `library.png` — Library browser (TV shows or movies grid)
- `admin.png` — Admin panel with user management and invite links
- `requests.png` — Media request system showing pending/completed requests

Recommended: 1400px wide, PNG format, cropped to the page content area (no browser chrome needed).
